# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the action will run. 
on:
  # Triggers the workflow on push or pull request events but only for the master branch
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "check_code_style"
  check_code_style:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE,  so your job can access it
      - uses: actions/checkout@v2

      # Runs a single command using the runners shell
      #- name: Run a one-line script
      #  run: echo Hello, world!

      # Runs a set of commands using the runners shell
      - name: check pycodestyle syntax
        run: |
          pip install pycodestyle
          # E501 ignore long lines (e.g. larger than 80 chars)
          # E722 do not use bare 'except'
          # W605 invalid escape sequence '\ ' 
          pycodestyle --ignore=E501,E722,W605 src/DeviceBuilder.py
          pycodestyle --ignore=E501,E722,W605 src/pki2include.py
          #pycodestyle --ignore=E501,E722,W605 src/swag2cbor.py
          #pycodestyle --ignore=E501,E722,W605 src/cbor2include.py
          #pycodestyle --ignore=E501,E722,W605 src/compareJson.py
